# Current runtime dependencies
fastapi==0.115.12
uvicorn[standard]==0.34.2
python-multipart==0.0.20
pydantic==2.11.4
pydantic-settings==2.9.1
python-dotenv==1.0.1

# HTTP client for calling external APIs
httpx==0.27.0

# YouTube transcript fetching — reads video captions for semantic matching
youtube-transcript-api==0.6.3

# TF-IDF vectorisation + cosine similarity for transcript-vs-query ranking
scikit-learn==1.4.2

# LLM — Google Gemini via official SDK
google-generativeai==0.8.4

# OCR — Tesseract wrapper + image processing
pytesseract==0.3.10
Pillow==10.3.0

# PDF handling — extract text + render pages as images for OCR
PyMuPDF==1.24.5
